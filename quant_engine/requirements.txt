# Python Compute Layer Requirements
# Phase 4.2: quant_engine/daemon.py 依赖
# 安装: pip install -r requirements.txt
# Stage 1C: 来源自源码直接 import（daemon.py + indicators/*）:
#   numpy, pandas:    所有指标计算必需
#   jsonschema:       协议校验（daemon.py 直接 import）
# note: rpds-py 是 jsonschema 的传递依赖，由 pip 自动解析

pandas>=2.0.0
numpy>=1.24.0
jsonschema>=4.20.0

# Phase 9D: immutable local Parquet storage and read-only derived views.
# Exact stable versions were verified against CPython 3.12 wheels.
duckdb==1.5.5
polars==1.44.1

# Maintained dependency audit gate used by CI; it grants no vulnerability exceptions.
pip-audit==2.10.1
