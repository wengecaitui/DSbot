export const DASHBOARD_HTML = `<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>DSbot Project Control Center</title>
  <link rel="stylesheet" href="/dashboard.css">
</head>
<body>
  <canvas id="ambientCanvas" aria-hidden="true"></canvas>
  <div class="shell">
    <aside class="sidebar">
      <div class="brand"><span>D</span><div><strong>DSbot</strong><small>Project Control Center</small></div></div>
      <nav>
        <a class="nav-item active" href="#project">项目状态</a>
        <a class="nav-item" href="#task">当前任务</a>
        <a class="nav-item" href="#activity">事件时间线</a>
        <a class="nav-item" href="#alerts">告警与整改</a>
        <a class="nav-item" href="#collaboration">协作上下文</a>
        <a class="nav-item" href="#technicalPanel">原始证据</a>
      </nav>
      <div class="boundary"><i></i><strong>本机只读</strong><small>不授予批准，不执行交易</small></div>
    </aside>

    <div class="workspace">
      <header class="topbar">
        <label class="search"><span>⌕</span><input id="search" type="search" placeholder="搜索任务、事件、文件或告警"><kbd>Ctrl K</kbd></label>
        <div class="actions">
          <button id="refreshButton" class="button">立即刷新</button>
          <button id="modeButton" class="button">原始证据</button>
          <button class="bell" title="告警">告警 <b id="bellCount">0</b></button>
          <div id="connection" class="connection"><i></i><span>连接中</span></div>
        </div>
      </header>

      <main>
        <section class="hero">
          <div><p>DSbot / Evidence-driven delivery</p><h1>Project Control Center</h1><span>状态只来自 Git、GitHub、测试退出码和 Hermes 可观察事件。</span></div>
          <div class="clock"><strong id="clock">--:--:--</strong><span id="lastUpdated">等待证据</span></div>
        </section>

        <section id="metricCards" class="metric-grid"></section>

        <section id="project" class="card project-card" aria-live="polite">
          <div class="heading"><div><h2>当前交付边界</h2><p>不使用主观百分比；证据缺失会保持 UNAVAILABLE。</p></div><span id="projectStatus" class="status">PLANNED</span></div>
          <div id="projectCenter" class="fact-grid"><div class="empty">等待项目证据。</div></div>
          <div class="two-column">
            <div><h3>授权边界</h3><div id="projectApprovals" class="approval-grid"></div></div>
            <div><h3>最近证据</h3><div id="projectTimeline" class="timeline"></div></div>
          </div>
        </section>

        <section class="content-grid">
          <div class="main-column">
            <section id="task" class="card">
              <div class="heading"><div><h2>当前任务</h2><p>阶段标记是观察事实，不是完成度估算。</p></div><span id="taskBadge" class="status muted">WAITING</span></div>
              <div id="currentTask" class="empty">尚未观察到 task ID。</div>
            </section>

            <section id="activity" class="card">
              <div class="heading"><div><h2>Agent / Runtime 事件时间线</h2><p id="activityCaption">实时更新</p></div><div id="filterChips" class="chips"><button class="chip active" data-filter="all">全部</button><button class="chip" data-filter="task">任务</button><button class="chip" data-filter="tool">工具</button><button class="chip" data-filter="change">代码</button><button class="chip" data-filter="error">错误</button></div></div>
              <div class="event-head"><span>活动</span><span>来源</span><span>结果</span><span>时间</span></div>
              <div id="events"></div>
            </section>

            <section id="alerts" class="card">
              <div class="heading"><div><h2>告警与整改建议</h2><p>建议不会自动修改代码。</p></div><span id="alertCount" class="status muted">0</span></div>
              <div class="alert-layout"><div id="alertList"></div><div id="recommendations"></div></div>
            </section>

            <section id="collaboration" class="card">
              <div class="heading"><div><h2>Codex × Hermes 协作上下文</h2><p>生成脱敏的只读 handoff；页面不会发送消息或执行命令。</p></div><span class="status muted">READ-ONLY</span></div>
              <label class="field">本轮目标<textarea id="collaborationObjective" maxlength="2000" placeholder="基于可观察证据说明目标和允许的动作边界"></textarea></label>
              <div class="collab-options">
                <label class="field">协作对象<select id="collaborationTarget"><option value="codex-hermes">Codex + Hermes</option><option value="codex">Codex</option><option value="hermes">Hermes</option></select></label>
                <label class="field">动作上限<select id="collaborationRisk"><option value="R0_READ_ONLY">R0 只读</option><option value="R1_REVERSIBLE_WORKSPACE_WRITE">R1 可逆工作区写入</option><option value="R2_STATEFUL_OPERATION">R2 外部状态操作</option></select></label>
              </div>
              <div class="collab-actions"><button id="generateCollaboration" class="button primary">生成协作包</button><button id="copyCollaboration" class="button">复制 Prompt</button><button id="downloadCollaboration" class="button">下载 JSON</button></div>
              <p class="safety">协作包不构成审批，不会直接发送消息或执行命令。R3、R4 与任何交易环境操作均不在此页面的能力范围内。</p>
              <pre id="collaborationPreview">尚未生成。</pre>
            </section>

            <section id="technicalPanel" class="card technical-panel">
              <div class="heading"><div><h2>原始技术证据</h2><p>脱敏后的 API 快照；不据此推断 Agent 隐藏思维。</p></div><span class="status muted">RAW</span></div>
              <pre id="technicalGrid">等待证据。</pre>
            </section>
          </div>

          <aside class="side-column">
            <section class="card"><div class="heading"><div><h2>运行健康</h2><p>Hermes 进程、端口与健康探针</p></div><span id="healthScore" class="status muted">UNKNOWN</span></div><div id="healthList"></div></section>
            <section class="card"><div class="heading"><div><h2>安全说明</h2><p>强制边界</p></div></div><ul class="safety-list"><li>Dashboard 只监听 127.0.0.1</li><li>GitHub / 测试不可用不等于通过</li><li>Shadow reference proof 不等于真实 Shadow</li><li>Paper / Testnet / Live 保持 false</li></ul></section>
            <section class="card quick"><button id="showTechnical">查看原始证据</button><button id="copyStatus">复制当前状态</button><button id="scrollTop">返回顶部</button></section>
          </aside>
        </section>
      </main>
    </div>
  </div>

  <dialog id="eventDialog">
    <form method="dialog"><button class="dialog-close">×</button></form>
    <p>OBSERVABLE EVIDENCE</p><h2 id="dialogTitle">事件详情</h2><p id="dialogExplain"></p>
    <div id="dialogFacts" class="dialog-facts"></div>
    <section><strong>证据说明</strong><div id="dialogCodeExplanation"></div></section>
    <details open><summary>原始技术证据（保持原始输出）</summary><pre id="dialogJson"></pre></details>
    <div class="dialog-actions"><button id="addToCollaborationButton" class="button">加入协作包</button><button id="copyExplanationButton" class="button">复制说明</button><button id="copyButton" class="button primary">复制原始证据</button></div>
  </dialog>
  <script src="/dashboard.js" defer></script>
</body>
</html>`;

export const DASHBOARD_CSS = `
:root{color-scheme:dark;--bg:#060914;--panel:rgba(16,24,48,.9);--panel2:rgba(10,16,34,.72);--line:rgba(145,166,244,.18);--text:#f4f7ff;--muted:#9ca9c7;--blue:#7290ff;--green:#45dcad;--amber:#ffc463;--red:#ff748a;--purple:#a98cff;--shadow:0 20px 60px rgba(0,0,0,.28)}*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;background:radial-gradient(circle at 50% -15%,#1b295b 0,#0b1128 31%,var(--bg) 66%);color:var(--text);font:14px/1.5 Inter,"Segoe UI","Microsoft YaHei",sans-serif}button,input,textarea,select{font:inherit}button{cursor:pointer}#ambientCanvas{position:fixed;inset:0;width:100%;height:100%;pointer-events:none;opacity:.55}.shell{position:relative;display:grid;grid-template-columns:225px minmax(0,1fr);gap:14px;padding:14px;min-height:100vh}.sidebar,.topbar,.card,.metric{background:linear-gradient(145deg,rgba(22,32,61,.95),rgba(10,16,34,.92));border:1px solid var(--line);box-shadow:var(--shadow);backdrop-filter:blur(18px)}.sidebar{position:sticky;top:14px;height:calc(100vh - 28px);border-radius:20px;padding:20px 14px;display:flex;flex-direction:column}.brand{display:flex;align-items:center;gap:10px;padding:0 8px 24px}.brand>span{display:grid;place-items:center;width:36px;height:36px;border-radius:12px;background:linear-gradient(145deg,#86a8ff,#586cf4);font-weight:900}.brand strong,.brand small{display:block}.brand small{color:var(--muted);font-size:9px}.sidebar nav{display:grid;gap:5px}.nav-item{padding:10px 12px;border-radius:10px;color:var(--muted);text-decoration:none;font-size:11px}.nav-item:hover,.nav-item.active{color:#fff;background:rgba(114,144,255,.14)}.boundary{margin-top:auto;padding:12px;border:1px solid var(--line);border-radius:11px;background:var(--panel2)}.boundary i{display:inline-block;width:7px;height:7px;border-radius:50%;background:var(--green);margin-right:6px}.boundary strong,.boundary small{display:block}.boundary small{color:var(--muted);font-size:8px;margin-top:4px}.workspace{min-width:0}.topbar{min-height:64px;border-radius:18px;padding:12px 16px;display:flex;justify-content:space-between;align-items:center;gap:12px}.search{display:flex;align-items:center;gap:8px;width:min(430px,45vw);border:1px solid var(--line);border-radius:10px;padding:8px 10px;background:var(--panel2)}.search input{flex:1;min-width:0;color:var(--text);background:transparent;border:0;outline:0}.search kbd{font-size:8px;color:var(--muted)}.actions{display:flex;align-items:center;gap:8px}.button,.bell,.quick button{border:1px solid var(--line);border-radius:9px;background:rgba(255,255,255,.045);color:var(--text);padding:8px 11px;font-size:10px}.button:hover,.bell:hover,.quick button:hover{border-color:rgba(145,166,244,.5)}.button.primary{background:var(--blue);border-color:transparent}.bell b{background:var(--red);border-radius:99px;padding:1px 5px}.connection{display:flex;gap:6px;align-items:center;color:var(--muted);font-size:9px}.connection i{width:7px;height:7px;border-radius:50%;background:var(--amber)}.connection.online i{background:var(--green)}.connection.offline i{background:var(--red)}main{max-width:1480px;margin:auto;padding:16px 8px 40px}.hero{display:flex;justify-content:space-between;align-items:end;padding:6px 4px 20px}.hero p{margin:0;color:var(--blue);font-size:9px;letter-spacing:.1em}.hero h1{margin:4px 0;font-size:25px}.hero span{color:var(--muted);font-size:11px}.clock{text-align:right}.clock strong,.clock span{display:block}.clock span{font-size:8px;color:var(--muted)}.metric-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:11px;margin-bottom:11px}.metric{border-radius:14px;padding:14px}.metric label{display:block;color:var(--muted);font-size:8px}.metric strong{display:block;font-size:19px;margin:8px 0 3px}.metric small{color:var(--muted);font-size:8px}.card{border-radius:15px;padding:16px}.project-card{margin-bottom:11px}.heading{display:flex;justify-content:space-between;gap:10px;align-items:start;margin-bottom:13px}.heading h2{font-size:13px;margin:0}.heading p{font-size:9px;color:var(--muted);margin:3px 0}.status{border-radius:99px;padding:4px 8px;background:rgba(114,144,255,.15);color:var(--blue);font:700 8px/1.4 ui-monospace,monospace}.status.muted{background:rgba(255,255,255,.06);color:var(--muted)}.fact-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px}.fact{min-width:0;border:1px solid var(--line);border-radius:9px;padding:9px;background:var(--panel2)}.fact.wide{grid-column:span 2}.fact label,.fact small{display:block;color:var(--muted);font-size:8px}.fact strong{display:block;margin:4px 0;font-size:10px;overflow-wrap:anywhere}.two-column{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-top:14px}.two-column h3{font-size:9px;color:var(--muted);text-transform:uppercase;letter-spacing:.08em}.approval-grid{display:grid;grid-template-columns:repeat(5,1fr);gap:6px}.approval{border:1px solid rgba(255,116,138,.25);border-radius:9px;padding:8px;background:rgba(255,116,138,.06)}.approval strong,.approval span,.approval small{display:block}.approval strong{font-size:8px}.approval span{font:700 8px ui-monospace,monospace;color:var(--red);margin:3px 0}.approval small{font-size:7px;color:var(--muted)}.timeline{display:grid;max-height:155px;overflow:auto}.timeline-row{display:grid;grid-template-columns:60px 1fr auto;gap:6px;padding:5px 0;border-bottom:1px solid var(--line);font-size:8px}.timeline-row time,.timeline-row span{color:var(--muted)}.pass{color:var(--green)!important}.fail{color:var(--red)!important}.content-grid{display:grid;grid-template-columns:minmax(0,1fr) 290px;gap:11px}.main-column,.side-column{display:grid;gap:11px;align-content:start}.task-steps{display:grid;grid-template-columns:repeat(4,1fr);gap:6px}.task-step{border:1px solid var(--line);border-radius:8px;padding:7px;text-align:center;color:var(--muted);font-size:8px}.task-step.done{color:var(--green);background:rgba(69,220,173,.06)}.task-meta h3{margin:2px 0}.task-meta p{color:var(--muted);font-size:9px}.chips{display:flex;gap:4px}.chip{border:0;border-radius:7px;background:rgba(255,255,255,.05);color:var(--muted);padding:5px 8px;font-size:8px}.chip.active{background:rgba(114,144,255,.16);color:var(--blue)}.event-head,.event-row{display:grid;grid-template-columns:minmax(0,1fr) 95px 80px 85px;gap:8px;align-items:center}.event-head{padding:0 9px 7px;color:var(--muted);font-size:8px}.event-row{padding:9px;border-top:1px solid var(--line);font-size:9px;cursor:pointer}.event-row:hover{background:rgba(114,144,255,.06)}.event-row strong,.event-row small{display:block}.event-row small{color:var(--muted)}.source,.result{color:var(--muted);font-size:8px}.alert-layout{display:grid;grid-template-columns:1fr 1fr;gap:10px}.alert,.recommendation{border:1px solid var(--line);border-radius:9px;padding:9px;margin-bottom:6px}.alert.critical{border-color:rgba(255,116,138,.35)}.alert strong,.alert small,.recommendation strong,.recommendation small{display:block}.alert small,.recommendation small{color:var(--muted);font-size:8px}.field{display:grid;gap:5px;color:var(--muted);font-size:9px}.field textarea,.field select{width:100%;border:1px solid var(--line);border-radius:9px;background:var(--panel2);color:var(--text);padding:9px;outline:0}.field textarea{min-height:80px;resize:vertical}.collab-options{display:grid;grid-template-columns:1fr 1fr;gap:9px;margin:9px 0}.collab-actions{display:flex;gap:7px}.safety{color:var(--amber);font-size:8px}.collaboration-preview,pre{white-space:pre-wrap;overflow-wrap:anywhere}.collaboration-preview,#collaborationPreview,.technical-panel pre,dialog pre{max-height:360px;overflow:auto;background:#070c19;border:1px solid var(--line);border-radius:9px;padding:11px;color:#c9d4ef;font:9px/1.55 ui-monospace,Consolas,monospace}.technical-panel{display:none}.technical .technical-panel{display:block}.health-row{display:grid;grid-template-columns:8px 1fr auto;gap:7px;padding:7px 0;border-bottom:1px solid var(--line);font-size:9px}.health-row i{width:7px;height:7px;border-radius:50%;background:var(--red)}.health-row i.ok{background:var(--green)}.health-row span{color:var(--muted)}.safety-list{padding-left:18px;color:var(--muted);font-size:9px}.safety-list li{margin:7px 0}.quick{display:grid;gap:7px}.quick button{text-align:left}.empty{color:var(--muted);font-size:9px;padding:12px}dialog{width:min(760px,92vw);border:1px solid var(--line);border-radius:15px;background:#0c1327;color:var(--text);padding:20px}dialog::backdrop{background:rgba(0,0,0,.65)}.dialog-close{position:absolute;right:12px;top:8px;border:0;background:transparent;color:var(--muted);font-size:22px}.dialog-facts{display:grid;grid-template-columns:repeat(2,1fr);gap:6px}.dialog-facts .fact{padding:7px}.dialog-actions{display:flex;justify-content:flex-end;gap:7px;margin-top:10px}
@media(max-width:1050px){.shell{grid-template-columns:78px 1fr}.brand div,.boundary small,.nav-item{font-size:0}.nav-item:before{content:"•";font-size:18px}.content-grid{grid-template-columns:1fr}.side-column{grid-template-columns:repeat(3,1fr)}.fact-grid{grid-template-columns:repeat(2,1fr)}}
@media(max-width:720px){.shell{display:block;padding:8px}.sidebar{display:none}.topbar,.hero{align-items:flex-start;flex-direction:column}.search{width:100%}.actions{flex-wrap:wrap}.metric-grid,.two-column,.alert-layout,.collab-options,.side-column{grid-template-columns:1fr}.fact-grid{grid-template-columns:1fr}.fact.wide{grid-column:auto}.approval-grid{grid-template-columns:repeat(2,1fr)}.event-head,.event-row{grid-template-columns:1fr auto}.event-head span:nth-child(2),.event-head span:nth-child(3),.event-row>span:nth-child(2),.event-row>span:nth-child(3){display:none}.chips{display:none}.task-steps{grid-template-columns:repeat(2,1fr)}}
`;

export const DASHBOARD_JS = `
const state={monitor:null,project:null,activity:{recentTasks:[]},events:[],alerts:[],recommendations:[],filter:'all',selected:null,collaborationContext:null,collaborationEvidence:new Set(),collaborationBundle:null,collaborationPrompt:''};
const $=id=>document.getElementById(id);
const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
const relative=value=>{if(!value)return'无时间';const seconds=Math.max(0,Math.round((Date.now()-new Date(value).getTime())/1000));if(seconds<5)return'刚刚';if(seconds<60)return seconds+' 秒前';if(seconds<3600)return Math.floor(seconds/60)+' 分钟前';return Math.floor(seconds/3600)+' 小时前'};
const runtime=()=>state.monitor?.lastEventBySource?.process?.after||{};
const currentTask=()=>state.activity?.currentTask;
function category(event){if(event.action==='log.error'||event.result?.ok===false)return'error';if(event.source==='filesystem'||event.source==='git')return'change';if(event.source==='tool'||event.action.startsWith('tool.'))return'tool';if(event.taskId||event.action.startsWith('task.'))return'task';return'other'}
function projectFact(label,value,detail,wide=false){return '<div class="fact '+(wide?'wide':'')+'"><label>'+esc(label)+'</label><strong>'+esc(value??'UNAVAILABLE')+'</strong><small>'+esc(detail||'')+'</small></div>'}
function renderMetrics(){const project=state.project,task=currentTask(),runtimeState=runtime();const checks=[runtimeState.gateway?.state==='running',runtimeState.health?.ok===true,(runtimeState.ports||[]).length>0&&(runtimeState.ports||[]).every(port=>port.listening),(runtimeState.processes||[]).some(process=>process.name==='Hermes')];const health=checks.length&&checks.every(Boolean)?'HEALTHY':checks.some(Boolean)?'DEGRADED':'UNKNOWN';const items=[['WORKFLOW',project?.status||'UNAVAILABLE',project?.currentCapability||'No project evidence'],['TASK',task?.status||'WAITING',task?.lastAction||'No task event'],['REMOTE CI',project?.ci?.status||'UNAVAILABLE',(project?.remoteTests||[]).length+' observed checks'],['RUNTIME',health,(state.alerts||[]).length+' active alerts']];$('metricCards').innerHTML=items.map(item=>'<article class="metric"><label>'+esc(item[0])+'</label><strong>'+esc(item[1])+'</strong><small>'+esc(item[2])+'</small></article>').join('')}
function renderProject(){const project=state.project;if(!project){$('projectStatus').textContent='UNAVAILABLE';$('projectCenter').innerHTML='<div class="empty">项目证据不可用；不会推断任何门禁已经通过。</div>';$('projectApprovals').innerHTML='';$('projectTimeline').innerHTML='';return}$('projectStatus').textContent=project.status;const repository=project.repository||{};const pullRequest=project.pullRequest;const localTests=project.localTests||[];const remoteTests=project.remoteTests||[];const missingChecks=project.ci?.missingRequiredChecks||[];$('projectCenter').innerHTML=[projectFact('Current capability',project.currentCapability,project.currentTask,true),projectFact('Active agent',project.activeAgent,'observable identity only'),projectFact('Branch',repository.branch,repository.worktree,true),projectFact('Commit SHA',repository.commitSha,'integration '+(repository.integrationHead||'UNAVAILABLE'),true),projectFact('Changed files',(repository.changedFiles||[]).length,(repository.changedFiles||[]).join(', ')||'clean'),projectFact('Pull request',pullRequest?'#'+pullRequest.number+' '+pullRequest.state:'NONE',pullRequest?.url||'No PR evidence'),projectFact('Local tests',localTests.length,localTests.map(test=>test.command+' => '+test.exitCode).join(', ')||'No SHA-bound exit-code evidence'),projectFact('Remote tests',remoteTests.length,(remoteTests.map(check=>check.name+': '+(check.conclusion||check.status)).join(', ')||'No check evidence')+(missingChecks.length?'; missing required: '+missingChecks.join(', '):'')),projectFact('Blockers',(project.blockers||[]).length,(project.blockers||[]).join('; ')||'None declared'),projectFact('Data gaps',(project.dataGaps||[]).length,(project.dataGaps||[]).join('; ')||'None',true),projectFact('Next action',project.nextAction,'evidence-gated',true)].join('');$('projectApprovals').innerHTML=Object.entries(project.approvals||{}).map(([name,approval])=>'<div class="approval"><strong>'+esc(name.toUpperCase())+'</strong><span>'+String(approval.approved).toUpperCase()+'</span><small>'+esc(approval.limitation)+'</small></div>').join('');const timeline=(project.eventTimeline||[]).slice(-8).reverse();$('projectTimeline').innerHTML=timeline.length?timeline.map(item=>'<div class="timeline-row"><time>'+new Date(item.timestamp).toLocaleTimeString()+'</time><strong>'+esc(item.action)+'</strong><span class="'+String(item.result).toLowerCase()+'">'+esc(item.result)+'</span></div>').join(''):'<div class="empty">本进程尚未观察到 Hermes/Codex 事件。</div>'}
function renderTask(){const task=currentTask();if(!task){$('taskBadge').textContent='WAITING';$('currentTask').innerHTML='<div class="empty">尚未观察到 task ID。</div>';return}$('taskBadge').textContent=task.status;const stages=[['TASK OBSERVED',task.stages.taskObserved],['TOOL OBSERVED',task.stages.toolObserved],['WORKSPACE CHANGED',task.stages.workspaceChanged],['COMPLETION OBSERVED',task.stages.completionObserved]];$('currentTask').innerHTML='<div class="task-meta"><small>'+esc(task.taskId)+'</small><h3>'+esc(task.lastAction)+'</h3><p>最近证据 '+relative(task.lastSeenAt)+'。下列阶段仅表示已观察，不表示主观完成度。</p></div><div class="task-steps">'+stages.map(stage=>'<div class="task-step '+(stage[1]?'done':'')+'">'+(stage[1]?'YES ':'NO ')+stage[0]+'</div>').join('')+'</div>'}
function renderHealth(){const data=runtime();const ports=data.ports||[],processes=data.processes||[];const rows=[['Gateway',data.gateway?.state==='running',data.gateway?.state||'unknown'],['Health API',data.health?.ok===true,data.health?.status?'HTTP '+data.health.status:'unavailable'],['Required ports',ports.length>0&&ports.every(port=>port.listening),ports.filter(port=>port.listening).length+'/'+ports.length],['Hermes process',processes.some(process=>process.name==='Hermes'),processes.filter(process=>process.name==='Hermes').length+' observed']];const healthy=rows.every(row=>row[1]);$('healthScore').textContent=healthy?'HEALTHY':rows.some(row=>row[1])?'DEGRADED':'UNKNOWN';$('healthList').innerHTML=rows.map(row=>'<div class="health-row"><i class="'+(row[1]?'ok':'')+'"></i><strong>'+esc(row[0])+'</strong><span>'+esc(row[2])+'</span></div>').join('')}
function filteredEvents(){const query=$('search').value.toLowerCase();return state.events.filter(event=>(state.filter==='all'||category(event)===state.filter)&&(!query||JSON.stringify([event.action,event.target,event.taskId]).toLowerCase().includes(query))).slice().reverse().slice(0,30)}
function renderEvents(){const events=filteredEvents();$('activityCaption').textContent='显示 '+events.length+' 条可观察事件';$('events').innerHTML=events.length?events.map(event=>'<div class="event-row" data-event="'+esc(event.eventId)+'"><div><strong>'+esc(event.action)+'</strong><small>'+esc(event.taskId||event.target||'observable event')+'</small></div><span class="source">'+esc(event.source)+'</span><span class="result '+(event.result?.ok===true?'pass':event.result?.ok===false?'fail':'')+'">'+(event.result?.ok===true?'PASS':event.result?.ok===false?'FAIL':'OBSERVED')+'</span><span class="source">'+relative(event.timestamp)+'</span></div>').join(''):'<div class="empty">没有符合条件的事件。</div>';document.querySelectorAll('[data-event]').forEach(row=>row.addEventListener('click',()=>openEvent(row.dataset.event)))}
function renderAlerts(){const alerts=state.alerts.slice().reverse().slice(0,8);$('alertCount').textContent=String(alerts.length);$('bellCount').textContent=String(alerts.length);$('alertList').innerHTML=alerts.length?alerts.map(alert=>'<div class="alert '+esc(alert.severity)+'"><strong>'+esc(alert.title)+'</strong><small>'+esc(alert.message)+' · '+alert.occurrences+' 次</small></div>').join(''):'<div class="empty">暂无告警。</div>';const recommendations=state.recommendations.slice().reverse().slice(0,8);$('recommendations').innerHTML=recommendations.length?recommendations.map(item=>'<div class="recommendation"><strong>'+esc(item.title)+'</strong><small>'+esc(item.diagnosis)+' · '+esc(item.status)+'</small></div>').join(''):'<div class="empty">暂无整改建议。</div>'}
function evidenceExplanation(event){return[['这是什么','编号 '+event.eventId+' 的可观察事件，动作是 '+event.action+'。'],['证据强度',event.evidenceLevel+'；只代表监控实际记录的证据。'],['结果',event.result?.ok===true?'记录为成功。':event.result?.ok===false?'记录为失败。':'没有明确成功或失败结果。'],['不能推出','不能据此推断 Agent 的隐藏思维、未记录动作或业务授权。']]}
function openEvent(id){const event=state.events.find(item=>item.eventId===id);if(!event)return;state.selected=event;$('dialogTitle').textContent=event.action;$('dialogExplain').textContent='来自 '+event.source+' 的可观察证据。';$('dialogFacts').innerHTML=[['时间',new Date(event.timestamp).toLocaleString()],['风险',event.riskClass],['任务 ID',event.taskId||'未关联'],['目标',event.target||'无']].map(item=>projectFact(item[0],item[1],'')).join('');$('dialogCodeExplanation').innerHTML=evidenceExplanation(event).map(item=>'<div class="fact"><strong>'+esc(item[0])+'</strong><small>'+esc(item[1])+'</small></div>').join('');$('dialogJson').textContent=JSON.stringify(event,null,2);$('addToCollaborationButton').textContent=state.collaborationEvidence.has(event.eventId)?'从协作包移除':'加入协作包';$('eventDialog').showModal()}
function redactDraft(value){return String(value||'').replace(/\bBearer\s+[A-Za-z0-9._~+/=-]+/gi,'Bearer [REDACTED]').replace(/\b(api[_-]?key|authorization|cookie|password|passwd|private[_-]?key|secret|token)\b\s*[:=]\s*[^\s,;]+/gi,(match,key)=>key+'=[REDACTED]')}
function buildCollaborationBundle(context){const evidence=state.events.filter(event=>state.collaborationEvidence.has(event.eventId));return{schemaVersion:'1.0',kind:'dsbot.collaboration.handoff',generatedAt:new Date().toISOString(),objective:redactDraft($('collaborationObjective').value).trim()||'基于可观察证据评估当前状态并提出安全下一步。',routing:{target:$('collaborationTarget').value,requestedActionCeiling:$('collaborationRisk').value},authorization:{thisBundleGrantsApproval:false,productionChangesRequireSeparateAuthorization:true},capabilities:{canExecuteCommands:false,canSendMessages:false,canPersistDrafts:false},source:{endpoint:'/api/collaboration-context',generatedAt:context.generatedAt,channel:context.channel},context:{project:context.project||null,task:context.activity?.currentTask||null,alerts:context.recentAlerts||[],recommendations:context.recommendations||[],evidence:evidence.length?evidence:evidence.concat(state.events.slice(-8))}}}
function collaborationPrompt(bundle){return['# DSbot 协作任务','','## 目标',bundle.objective,'','## 路由',bundle.routing.target,'动作上限：'+bundle.routing.requestedActionCeiling,'','## 强制安全边界','- 本协作包不构成审批或授权。','- 先验证事实，再区分 observed 与 inferred。','- 不得启动 Replay、Shadow、Paper、Testnet 或 Live。','- 任何写操作需保留回滚与验证证据。','','## 可追溯上下文',JSON.stringify(bundle,null,2)].join('\n')}
async function generateCollaboration(){const response=await fetch('/api/collaboration-context');if(!response.ok)throw new Error('HTTP '+response.status);state.collaborationContext=await response.json();state.collaborationBundle=buildCollaborationBundle(state.collaborationContext);state.collaborationPrompt=collaborationPrompt(state.collaborationBundle);$('collaborationPreview').textContent=state.collaborationPrompt;return true}
async function copyWithFeedback(button,value){const original=button.textContent;try{await navigator.clipboard.writeText(value);button.textContent='已复制'}catch{button.textContent='复制失败'}setTimeout(()=>button.textContent=original,1200)}
function renderTechnical(){ $('technicalGrid').textContent=JSON.stringify({project:state.project,monitor:state.monitor,activity:state.activity,alerts:state.alerts,recommendations:state.recommendations},null,2) }
function render(){renderMetrics();renderProject();renderTask();renderHealth();renderEvents();renderAlerts();renderTechnical();$('lastUpdated').textContent='最近刷新 '+new Date().toLocaleTimeString()}
async function refresh(){try{const [stateResponse,projectResponse]=await Promise.all([fetch('/api/state'),fetch('/api/project')]);if(!stateResponse.ok)throw new Error('state HTTP '+stateResponse.status);const data=await stateResponse.json();state.monitor=data.monitor;state.activity=data.activity||{recentTasks:[]};state.events=data.recentEvents||[];state.alerts=data.recentAlerts||[];state.recommendations=data.recommendations||[];state.project=projectResponse.ok?await projectResponse.json():data.project||null;$('connection').className='connection online';$('connection').innerHTML='<i></i><span>只读连接</span>';render()}catch{$('connection').className='connection offline';$('connection').innerHTML='<i></i><span>连接中断</span>'}}
function setTechnical(enabled){document.body.classList.toggle('technical',enabled);$('modeButton').setAttribute('aria-pressed',String(enabled));$('modeButton').textContent=enabled?'隐藏原始证据':'原始证据';if(enabled)$('technicalPanel').scrollIntoView({behavior:'smooth'})}
function initAmbient(){const canvas=$('ambientCanvas'),context=canvas.getContext('2d'),reduced=matchMedia('(prefers-reduced-motion: reduce)').matches,particles=[];let width=0,height=0,frame=0;function resize(){width=innerWidth;height=innerHeight;const ratio=Math.min(devicePixelRatio||1,1.5);canvas.width=width*ratio;canvas.height=height*ratio;context.setTransform(ratio,0,0,ratio,0,0);particles.length=0;for(let index=0;index<(reduced?20:55);index++)particles.push({x:Math.random()*width,y:Math.random()*height,vx:(Math.random()-.5)*.18,vy:(Math.random()-.5)*.18})}function draw(){context.clearRect(0,0,width,height);particles.forEach(particle=>{if(!reduced){particle.x=(particle.x+particle.vx+width)%width;particle.y=(particle.y+particle.vy+height)%height}context.beginPath();context.fillStyle='rgba(130,155,255,.25)';context.arc(particle.x,particle.y,1,0,Math.PI*2);context.fill()});if(!reduced)frame=requestAnimationFrame(draw)}addEventListener('resize',()=>{cancelAnimationFrame(frame);resize();draw()},{passive:true});resize();draw()}
$('refreshButton').addEventListener('click',refresh);$('modeButton').addEventListener('click',()=>setTechnical(!document.body.classList.contains('technical')));document.querySelector('.bell').addEventListener('click',()=>$('alerts').scrollIntoView({behavior:'smooth'}));$('search').addEventListener('input',renderEvents);document.querySelectorAll('.nav-item').forEach(item=>item.addEventListener('click',()=>{document.querySelectorAll('.nav-item').forEach(other=>other.classList.remove('active'));item.classList.add('active')}));document.querySelectorAll('.chip').forEach(item=>item.addEventListener('click',()=>{document.querySelectorAll('.chip').forEach(other=>other.classList.remove('active'));item.classList.add('active');state.filter=item.dataset.filter;renderEvents()}));$('showTechnical').addEventListener('click',()=>setTechnical(true));$('copyStatus').addEventListener('click',()=>copyWithFeedback($('copyStatus'),JSON.stringify({project:state.project,monitor:state.monitor,activity:state.activity,alerts:state.alerts},null,2)));$('scrollTop').addEventListener('click',()=>scrollTo({top:0,behavior:'smooth'}));$('addToCollaborationButton').addEventListener('click',()=>{if(!state.selected)return;const id=state.selected.eventId;if(state.collaborationEvidence.has(id))state.collaborationEvidence.delete(id);else state.collaborationEvidence.add(id);$('addToCollaborationButton').textContent=state.collaborationEvidence.has(id)?'从协作包移除':'加入协作包'});$('generateCollaboration').addEventListener('click',()=>generateCollaboration().catch(error=>$('collaborationPreview').textContent='生成失败：'+error.message));$('copyCollaboration').addEventListener('click',async()=>{try{await generateCollaboration();await copyWithFeedback($('copyCollaboration'),state.collaborationPrompt)}catch(error){$('collaborationPreview').textContent='生成失败：'+error.message}});$('downloadCollaboration').addEventListener('click',async()=>{try{await generateCollaboration();const blob=new Blob([JSON.stringify(state.collaborationBundle,null,2)],{type:'application/json'}),url=URL.createObjectURL(blob),link=document.createElement('a');link.href=url;link.download='dsbot-collaboration-'+new Date().toISOString().replace(/[:.]/g,'-')+'.json';link.click();setTimeout(()=>URL.revokeObjectURL(url),0)}catch(error){$('collaborationPreview').textContent='生成失败：'+error.message}});$('copyExplanationButton').addEventListener('click',()=>{if(state.selected)copyWithFeedback($('copyExplanationButton'),evidenceExplanation(state.selected).map(item=>item[0]+'：'+item[1]).join('\n'))});$('copyButton').addEventListener('click',()=>{if(state.selected)copyWithFeedback($('copyButton'),JSON.stringify(state.selected,null,2))});document.addEventListener('keydown',event=>{if(event.ctrlKey&&event.key.toLowerCase()==='k'){event.preventDefault();$('search').focus()}});setInterval(()=>$('clock').textContent=new Date().toLocaleTimeString(),1000);initAmbient();
let refreshTimer;const schedule=()=>{clearTimeout(refreshTimer);refreshTimer=setTimeout(refresh,150)};const stream=new EventSource('/api/events');stream.addEventListener('open',()=>{$('connection').className='connection online'});['observable','alert','recommendation'].forEach(name=>stream.addEventListener(name,schedule));stream.addEventListener('error',()=>{$('connection').className='connection offline'});refresh();
`.replaceAll("join('\n')", "join(String.fromCharCode(10))");
